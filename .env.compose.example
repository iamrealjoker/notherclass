# .env local para el puerto del host (lo usa docker-compose.yml)
# Cópialo a `.env` y ajusta. NUNCA subas el `.env` de verdad a git.

# Puerto del host donde se publica la consola web
TW_PORT_HOST=5000

# Zona horaria
TZ=Europe/Madrid
